-- [[ ROBLOX ELITE SCRIPT V6: ULTRA FARM & STEALTH + BOTÃO UI + ANTI-AFK + AUTO CLICKER ]]
-- Criado para fins educacionais e de estudo de lógica em Lua.

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser") -- Para o Anti-AFK Real
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- [[ PROTEÇÃO ANTI-DETECÇÃO AVANÇADA (ANTI-KICK & ANTI-BAN) ]]
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}

    -- Anti-Kick: Intercepta o comando de expulsão
    if tostring(method) == "Kick" or tostring(method) == "kick" then
        warn("[MANUS PROTECT] Bloqueada tentativa de Kick do servidor.")
        return wait(9e9) -- Trava o sinal de expulsão
    end
    
    -- Anti-Ban: Bloqueia RemoteEvents de detecção comuns
    if tostring(method) == "FireServer" then
        local remoteName = tostring(self.Name):lower()
        if remoteName:find("kick") or remoteName:find("ban") or remoteName:find("cheat") or remoteName:find("detect") then
            warn("[MANUS PROTECT] Bloqueado RemoteEvent de detecção: " .. self.Name)
            return nil
        end
    end

    return oldNamecall(self, unpack(args))
end)

-- Anti-Ban: Oculta mudanças na Câmera para o jogo
local oldIndex
oldIndex = hookmetamethod(game, "__index", function(self, key)
    if not checkcaller() then
        if key == "CFrame" and self:IsA("Camera") then
            return oldIndex(self, key)
        end
    end
    return oldIndex(self, key)
end)

-- [[ VARIÁVEIS DE ESTADO ]]
local espLinesEnabled = false
local espBoxEnabled = false
local espSkeletonEnabled = false
local aimbotEnabled = false
local showFOV = false
local infJumpEnabled = false
local flyEnabled = false
local autoClickerEnabled = false
local antiAFKEnabled = true -- Já vem ativado conforme pedido
local flySpeed = 50

-- [[ CONFIGURAÇÃO DO FOV ]]
local fovCircle = Drawing.new("Circle")
fovCircle.Visible = false
fovCircle.Radius = 60
fovCircle.Color = Color3.fromRGB(0, 255, 255)
fovCircle.Thickness = 1
fovCircle.Filled = false
fovCircle.Transparency = 1

-- [[ CONFIGURAÇÃO DA UI ]]
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local TitleLabel = Instance.new("TextLabel")
local CloseButton = Instance.new("TextButton")
local OpenButton = Instance.new("TextButton") -- Botão para abrir/fechar na tela
local ScrollingFrame = Instance.new("ScrollingFrame")
local UIListLayout = Instance.new("UIListLayout")

ScreenGui.Name = "ManusElite_V6"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

-- Botão Abrir/Fechar Flutuante
OpenButton.Name = "OpenButton"
OpenButton.Parent = ScreenGui
OpenButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
OpenButton.BorderSizePixel = 2
OpenButton.BorderColor3 = Color3.fromRGB(0, 255, 255)
OpenButton.Position = UDim2.new(0, 10, 0.5, -20)
OpenButton.Size = UDim2.new(0, 80, 0, 40)
OpenButton.Text = "MENU"
OpenButton.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenButton.Font = Enum.Font.GothamBold
OpenButton.TextSize = 14
OpenButton.Draggable = true

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -175)
MainFrame.Size = UDim2.new(0, 300, 0, 380)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true -- Começa aberto

TitleLabel.Name = "Title"
TitleLabel.Parent = MainFrame
TitleLabel.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
TitleLabel.Size = UDim2.new(1, 0, 0, 45)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "MANUS ELITE V6 - ULTRA"
TitleLabel.TextColor3 = Color3.fromRGB(0, 255, 255)
TitleLabel.TextSize = 18

CloseButton.Name = "CloseButton"
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseButton.Position = UDim2.new(1, -35, 0, 7)
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold

ScrollingFrame.Name = "ScrollingFrame"
ScrollingFrame.Parent = MainFrame
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.Position = UDim2.new(0, 10, 0, 55)
ScrollingFrame.Size = UDim2.new(1, -20, 1, -65)
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 2, 0)
ScrollingFrame.ScrollBarThickness = 4

UIListLayout.Parent = ScrollingFrame
UIListLayout.Padding = UDim.new(0, 10)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function createToggle(name, text, default, callback)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Parent = ScrollingFrame
    btn.Size = UDim2.new(0.95, 0, 0, 40)
    btn.BackgroundColor3 = default and Color3.fromRGB(0, 120, 120) or Color3.fromRGB(40, 40, 40)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = text .. ": " .. (default and "ON" or "OFF")
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.BorderSizePixel = 0
    
    local enabled = default
    btn.MouseButton1Click:Connect(function()
        enabled = not enabled
        btn.Text = text .. ": " .. (enabled and "ON" or "OFF")
        btn.BackgroundColor3 = enabled and Color3.fromRGB(0, 120, 120) or Color3.fromRGB(40, 40, 40)
        callback(enabled)
    end)
    return btn
end

-- FECHAR/ABRIR UI
local function toggleUI() MainFrame.Visible = not MainFrame.Visible end
CloseButton.MouseButton1Click:Connect(toggleUI)
OpenButton.MouseButton1Click:Connect(toggleUI)
UserInputService.InputBegan:Connect(function(i, g) if not g and i.KeyCode == Enum.KeyCode.Insert then toggleUI() end end)

-- [[ FUNÇÕES ULTRA FARM ]]
-- Anti-AFK Real (Simula atividade no motor do jogo)
LocalPlayer.Idled:Connect(function()
    if antiAFKEnabled then
        VirtualUser:Button2Down(Vector2.new(0,0), Camera.CFrame)
        wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), Camera.CFrame)
    end
end)

-- Auto Clicker (25ms)
spawn(function()
    while wait(0.025) do -- 25ms
        if autoClickerEnabled then
            VirtualUser:Button1Down(Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2), Camera.CFrame)
            wait(0.01)
            VirtualUser:Button1Up(Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2), Camera.CFrame)
        end
    end
end)

-- Infinite Jump
UserInputService.JumpRequest:Connect(function()
    if infJumpEnabled then
        LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
    end
end)

-- Fly Logic
local flyBodyVel, flyBodyGyro
local function startFly()
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local hrp = char.HumanoidRootPart
    flyBodyVel = Instance.new("BodyVelocity", hrp)
    flyBodyVel.Velocity = Vector3.new(0, 0, 0)
    flyBodyVel.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    flyBodyGyro = Instance.new("BodyGyro", hrp)
    flyBodyGyro.P = 9e4
    flyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    flyBodyGyro.CFrame = hrp.CFrame
end

local function stopFly()
    if flyBodyVel then flyBodyVel:Destroy() end
    if flyBodyGyro then flyBodyGyro:Destroy() end
end

RunService.RenderStepped:Connect(function()
    if flyEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        local camCF = Camera.CFrame
        local dir = Vector3.new(0, 0, 0)
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + camCF.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - camCF.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - camCF.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + camCF.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then dir = dir - Vector3.new(0, 1, 0) end
        flyBodyVel.Velocity = dir * flySpeed
        flyBodyGyro.CFrame = camCF
    end
end)

-- [[ LÓGICA ESP (SKELETON, BOX, LINES, TEAM COLORS) ]]
local objects = {}
local function createDrawings(player)
    local d = {Line = Drawing.new("Line"), Box = Drawing.new("Square"), Skeleton = {}}
    for i=1, 10 do d.Skeleton[i] = Drawing.new("Line") end
    d.Line.Thickness = 1
    d.Box.Thickness = 1
    d.Box.Filled = false
    objects[player] = d
end

local function updateESP()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            if not objects[p] then createDrawings(p) end
            local d = objects[p]
            local char = p.Character
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChild("Humanoid")
            local head = char:FindFirstChild("Head")
            local teamColor = p.TeamColor.Color or Color3.fromRGB(255, 255, 255)
            if hum and hum.Health > 0 and hrp and head then
                local hrpPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                if onScreen then
                    d.Line.Visible = espLinesEnabled
                    d.Line.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                    d.Line.To = Vector2.new(hrpPos.X, hrpPos.Y)
                    d.Line.Color = teamColor
                    d.Box.Visible = espBoxEnabled
                    local headPos = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.5, 0))
                    local legPos = Camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3, 0))
                    d.Box.Size = Vector2.new(2500 / hrpPos.Z, headPos.Y - legPos.Y)
                    d.Box.Position = Vector2.new(hrpPos.X - d.Box.Size.X / 2, hrpPos.Y - d.Box.Size.Y / 2)
                    d.Box.Color = teamColor
                else
                    d.Line.Visible = false
                    d.Box.Visible = false
                end
                if espSkeletonEnabled and onScreen then
                    local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
                    if torso then
                        local p1 = Camera:WorldToViewportPoint(head.Position)
                        local p2 = Camera:WorldToViewportPoint(torso.Position)
                        d.Skeleton[1].Visible = true
                        d.Skeleton[1].From = Vector2.new(p1.X, p1.Y)
                        d.Skeleton[1].To = Vector2.new(p2.X, p2.Y)
                        d.Skeleton[1].Color = teamColor
                    else d.Skeleton[1].Visible = false end
                else
                    for i=1, #d.Skeleton do d.Skeleton[i].Visible = false end
                end
            else
                d.Line.Visible = false
                d.Box.Visible = false
                for i=1, #d.Skeleton do d.Skeleton[i].Visible = false end
            end
        elseif objects[p] then
            objects[p].Line.Visible = false
            objects[p].Box.Visible = false
            for i=1, #objects[p].Skeleton do objects[p].Skeleton[i].Visible = false end
        end
    end
end

-- [[ LÓGICA AIMBOT ]]
local function isVisible(part)
    local char = LocalPlayer.Character
    if not char then return false end
    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {char, Camera}
    params.FilterType = Enum.RaycastFilterType.Exclude
    local result = workspace:Raycast(Camera.CFrame.Position, (part.Position - Camera.CFrame.Position).Unit * 1000, params)
    return result and result.Instance:IsDescendantOf(part.Parent)
end

local function getClosest()
    local target, dist = nil, math.huge
    local center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
            local head = p.Character.Head
            if isVisible(head) then
                local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
                if onScreen then
                    local d = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                    if d <= fovCircle.Radius and d < dist then
                        target, dist = p, d
                    end
                end
            end
        end
    end
    return target
end

-- [[ CRIAÇÃO DOS BOTÕES ]]
createToggle("AimbotToggle", "Aimbot (Head)", false, function(v) aimbotEnabled = v end)
createToggle("FOVToggle", "Show FOV (60)", false, function(v) showFOV = v end)
createToggle("LineToggle", "ESP Lines", false, function(v) espLinesEnabled = v end)
createToggle("BoxToggle", "ESP Box", false, function(v) espBoxEnabled = v end)
createToggle("SkeletonToggle", "ESP Skeleton", false, function(v) espSkeletonEnabled = v end)
createToggle("JumpToggle", "Infinite Jump", false, function(v) infJumpEnabled = v end)
createToggle("FlyToggle", "Fly (Voo)", false, function(v) flyEnabled = v if v then startFly() else stopFly() end end)
createToggle("ClickerToggle", "Auto Clicker (25ms)", false, function(v) autoClickerEnabled = v end)
createToggle("AFKToggle", "Anti-AFK Real", true, function(v) antiAFKEnabled = v end)

-- [[ LOOP PRINCIPAL ]]
RunService.RenderStepped:Connect(function()
    updateESP()
    fovCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    fovCircle.Visible = showFOV
    if aimbotEnabled then
        local t = getClosest()
        if t and t.Character and t.Character:FindFirstChild("Head") then
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, t.Character.Head.Position)
        end
    end
end)

Players.PlayerRemoving:Connect(function(p) 
    if objects[p] then 
        objects[p].Line:Remove() 
        objects[p].Box:Remove() 
        for i=1, #objects[p].Skeleton do objects[p].Skeleton[i]:Remove() end
        objects[p] = nil 
    end 
end)

